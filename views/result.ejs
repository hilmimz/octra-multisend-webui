<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <h2>Multisend Results</h2>
  <ul>
    <% results.forEach(r => { %>
      <li>
        <%= r.to %>: 
        <% if (r.success) { %>
          ✅ Sent (<%= r.hash %>)
        <% } else { %>
          ❌ Failed: <%= r.error %>
        <% } %>
      </li>
    <% }) %>
  </ul>
</body>
</html>