<%- include('layouts/header') %>
<section class="mx-auto p-10 m-5 shadow-2xl min-w-[700px] max-w-[700px]">
    <h1 class="text-2xl text-center font-bold mb-5 text-sky-800">Octra Multisender</h1>
    <div class="space-y-1">
      <p class="font-light text-justify">This application is designed to perform multisend transactions on the Octra network. Its backend logic is adapted from the official Octra implementation.</p>
      <p class="font-semibold text-justify text-sky-700">How to use?</p>
      <ol class="font-normal text-md list-decimal list-inside">
        <li><span class="font-light">Specify the amount of OCT you want to send to each address.</span></li>
        <li><span class="font-light">Paste multiple recipient addresses into the input box. Each address must be on a new line.</span></li>
        <li><span class="font-light">Once everything is filled in, click the "Send" button to initiate the multisend transaction.</span></li>
      </ol>
    </div>
    <div class="mt-3 bg-emerald-100 p-4 rounded-md space-y-4 border border-emerald-300">
      <p class="font-semibold">Address: <span class="font-light"><%= address ? address : 'N/A' %></span></p>
      <p class="font-semibold">Balance: <span class="font-light"><%= balance ? balance.toFixed(6) + ' OCT' : 'N/A' %></span></p>
    </div>
    <form action="/multisend" method="post" class="flex flex-col mt-4 space-y-5">
      <div>
        <label for="amount" class="block mb-2 text-sm font-medium text-gray-900">Amount</label>
        <input type="number" name="amount" step=".0001" id="" class="rounded-md border border-gray-400 text-sm focus:border-sky-600 focus:outline p-2">
      </div>
      <div class="mt-2">
        <label for="addresses" class="block mb-2 text-sm font-medium text-gray-900">Address (Seperate with newline)</label>
        <textarea name="addresses" id="" class="h-40 w-full border border-gray-400 rounded-md focus:border-sky-600 focus:outline p-2"></textarea>
      </div>
      <button type="submit" class="border-2 rounded-lg bg-blue-500 hover:bg-blue-700 text-white p-2">Send</button>
    </form>
</section>
<%- include('layouts/footer') %>
